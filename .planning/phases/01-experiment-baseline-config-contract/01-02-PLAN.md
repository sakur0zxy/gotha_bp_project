---
phase: 01-experiment-baseline-config-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - config/default_config.m
  - src/bp_validate_config.m
  - src/bp_data_pipeline.m
  - src/bp_output_pipeline.m
  - cs_echo_recovery/cs_default_config.m
  - .gitignore
  - main_gotha_bp.ipynb
autonomous: true
requirements:
  - IMG-02
  - REP-01
user_setup: []
must_haves:
  truths:
    - "正式主流程支持显式 `cfg.path.dataRoot`，未提供时才回退到候选目录查找。"
    - "主流程默认 headless，但在请求保存产物时仍能导出需要的图片和分析结果。"
    - "notebook 不再持有主流程实现，运行产物默认不纳入版本控制。"
  artifacts:
    - path: "config/default_config.m"
      provides: "显式 dataRoot 字段与 headless 默认值"
      contains: "config.path.dataRoot"
    - path: "src/bp_data_pipeline.m"
      provides: "显式路径优先的数据根目录解析"
      contains: "dataRootCandidates"
    - path: "src/bp_output_pipeline.m"
      provides: "headless 下仍可保存分析图片"
      contains: "figure('Visible', 'off'"
    - path: ".gitignore"
      provides: "运行产物默认忽略规则"
      contains: "cs_echo_recovery/results/"
    - path: "main_gotha_bp.ipynb"
      provides: "交互 wrapper 而非第二套主逻辑"
      contains: "main_gotha_bp("
  key_links:
    - from: "config/default_config.m"
      to: "src/bp_data_pipeline.m"
      via: "cfg.path.dataRoot / cfg.path.dataRootCandidates"
      pattern: "dataRoot"
    - from: "config/default_config.m"
      to: "src/bp_output_pipeline.m"
      via: "headless 默认值与保存策略分离"
      pattern: "savePointAnalysisImage"
    - from: "main_gotha_bp.ipynb"
      to: "main_gotha_bp.m"
      via: "notebook 调用单一正式入口"
      pattern: "main_gotha_bp\\("
---

<objective>
规范数据路径发现、正式运行默认值、入口角色和运行产物约定。

Purpose: 把“在哪找数据、默认怎么跑、哪些产物应该留下、notebook 扮演什么角色”固定成明确工程契约。
Output: 显式 `dataRoot` 支持、主流程 headless 默认、headless 可导出产物、notebook 薄包装、运行产物忽略规则。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md
@.planning/phases/01-experiment-baseline-config-contract/01-RESEARCH.md
@config/default_config.m
@src/bp_validate_config.m
@src/bp_data_pipeline.m
@src/bp_output_pipeline.m
@main_gotha_bp.m
@main_gotha_bp.ipynb
@cs_echo_recovery/cs_default_config.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为主流程增加显式 dataRoot 并实现路径优先级</name>
  <files>config/default_config.m, src/bp_validate_config.m, src/bp_data_pipeline.m</files>
  <read_first>config/default_config.m, src/bp_validate_config.m, src/bp_data_pipeline.m, README.md</read_first>
  <action>在 `config/default_config.m` 中新增正式字段 `config.path.dataRoot`，默认留空；保留 `config.path.dataRootCandidates` 作为兜底。更新 `bp_validate_config.m`：`cfg.path.dataRoot` 必须为空或文本。更新 `bp_data_pipeline.m`：当 `cfg.path.dataRoot` 非空时优先使用它；若是相对路径，则相对 `workspaceRoot` 解析；若显式路径下缺少首个必需数据文件，错误消息必须带上显式路径、缺失文件名和“未回退到候选目录”的原因。只有 `dataRoot` 为空时才遍历 `dataRootCandidates`。</action>
  <verify>Get-Content -Encoding UTF8 config\default_config.m,src\bp_validate_config.m,src\bp_data_pipeline.m | Select-String -Pattern "config.path.dataRoot","cfg.path.dataRoot","dataRootCandidates"</verify>
  <acceptance_criteria>
    - `config/default_config.m` 定义了 `config.path.dataRoot`
    - `src/bp_validate_config.m` 校验 `cfg.path.dataRoot` 为空或文本
    - `src/bp_data_pipeline.m` 的解析顺序是显式 `dataRoot` 优先、候选目录兜底
  </acceptance_criteria>
  <done>研究者可以用显式路径稳定定位数据集，不再只能依赖工作区约定。</done>
</task>

<task type="auto">
  <name>Task 2: 把主流程默认值切到 headless 并解耦图片保存</name>
  <files>config/default_config.m, src/bp_output_pipeline.m, cs_echo_recovery/cs_default_config.m</files>
  <read_first>config/default_config.m, src/bp_output_pipeline.m, cs_echo_recovery/cs_default_config.m, .planning/codebase/CONCERNS.md</read_first>
  <action>将 `default_config.m` 的正式主流程默认值切为 headless：至少把 `showInterruptedEcho=false`、`showProgress=false`、`analysis.pointAnaCfg.showFigures=false` 固定为默认值。随后修改 `bp_output_pipeline.m`，让点目标分析图片保存只受 `config.output.savePointAnalysisImage` 控制，不再依赖 `showFigures=true`；导出图片时统一使用不可见 figure，保证主流程在 batch/headless 模式下仍能保存分析图。同步清理 `cs_default_config.m` 中仅为“补锅主流程默认交互”而存在的冗余覆盖，保留真正的恢复实验差异。</action>
  <verify>Get-Content -Encoding UTF8 config\default_config.m,src\bp_output_pipeline.m,cs_echo_recovery\cs_default_config.m | Select-String -Pattern "showInterruptedEcho = false","showProgress = false","showFigures', false","Visible', 'off'"</verify>
  <acceptance_criteria>
    - `config/default_config.m` 的正式主流程默认值是 headless
    - `src/bp_output_pipeline.m` 不再把图片导出绑定到 `showFigures`
    - `cs_echo_recovery/cs_default_config.m` 不再依赖修补主流程错误默认值来实现 headless
  </acceptance_criteria>
  <done>正式主流程默认适合批处理运行，同时仍能保留应保存的科研产物。</done>
</task>

<task type="auto">
  <name>Task 3: 收口 notebook 角色并默认忽略运行产物</name>
  <files>.gitignore, main_gotha_bp.ipynb</files>
  <read_first>main_gotha_bp.ipynb, .planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md, README.md</read_first>
  <action>新增仓库级 `.gitignore`，至少忽略 `/img/` 和 `/cs_echo_recovery/results/` 两类正式运行产物。把 `main_gotha_bp.ipynb` 改成交互式薄包装：保留需要的说明/探索单元，但去掉重复的主流程实现，改为路径初始化、构造 `userCfg`、调用 `main_gotha_bp(userCfg)`。如果 notebook 里已有用户探索内容，保留解释性和实验性内容，只移除重复主逻辑，不要粗暴重置整本 notebook。</action>
  <verify>Get-Content -Encoding UTF8 .gitignore,main_gotha_bp.ipynb | Select-String -Pattern "img/","cs_echo_recovery/results/","main_gotha_bp\\("</verify>
  <acceptance_criteria>
    - `.gitignore` 包含 `/img/` 与 `/cs_echo_recovery/results/`
    - `main_gotha_bp.ipynb` 中存在对 `main_gotha_bp(...)` 的调用
    - notebook 不再包含完整 `function result = main_gotha_bp` 主体拷贝
  </acceptance_criteria>
  <done>正式入口收口到 `.m` 文件，运行产物不再持续污染 Git 工作区。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 在 MATLAB 中以 `cfg.path.dataRoot = 'definitely_missing_path'` 运行 `main_gotha_bp(cfg)`，确认失败消息明确指出显式路径和缺失文件
- [ ] 构造最小 `anaResult` / `anaInfo` 假数据并调用 `bp_output_pipeline('save_point_analysis', ...)`，确认 `showFigures=false` 且 `savePointAnalysisImage=true` 时仍会写出图片
- [ ] 检查 `.gitignore`、`default_config.m` 和 notebook 文本，确认显式路径、headless 默认和薄包装调用都已落地
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 数据路径规则、正式默认值和入口边界都被固定为代码契约
- 运行产物默认不再进入版本控制
  </success_criteria>

<output>
After completion, create `.planning/phases/01-experiment-baseline-config-contract/01-02-SUMMARY.md`
</output>
