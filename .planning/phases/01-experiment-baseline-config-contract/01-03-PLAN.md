---
phase: 01-experiment-baseline-config-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - examples/run_bp_minimal.m
  - examples/run_cs_recovery_minimal.m
  - docs/config_contract.md
autonomous: true
requirements:
  - REP-01
  - IMG-02
  - QLT-01
user_setup: []
must_haves:
  truths:
    - "研究者只看仓库内文档就能知道数据应放哪里、怎么运行主流程和恢复流程、输出会到哪里。"
    - "最小示例展示了如何通过配置覆盖运行实验，而不需要修改生产源码。"
    - "文档明确说明非法配置的失败语义和 notebook 的辅助角色。"
  artifacts:
    - path: "README.md"
      provides: "面向研究者的总入口说明"
      contains: "cfg.path.dataRoot"
    - path: "examples/run_bp_minimal.m"
      provides: "主流程最小运行示例"
      contains: "main_gotha_bp"
    - path: "examples/run_cs_recovery_minimal.m"
      provides: "恢复流程最小运行示例"
      contains: "run_cs_echo_recovery_demo"
    - path: "docs/config_contract.md"
      provides: "配置契约与错误语义参考"
      contains: "严格失败"
  key_links:
    - from: "README.md"
      to: "examples/run_bp_minimal.m"
      via: "README 引导用户查看和复用最小示例"
      pattern: "examples/run_bp_minimal.m"
    - from: "README.md"
      to: "docs/config_contract.md"
      via: "总览文档链接到配置契约细则"
      pattern: "config_contract"
    - from: "examples/run_cs_recovery_minimal.m"
      to: "cs_echo_recovery/run_cs_echo_recovery_demo.m"
      via: "恢复示例直接调用正式恢复入口"
      pattern: "run_cs_echo_recovery_demo\\("
---

<objective>
补齐面向研究者的基线实验说明文档和最小运行示例。

Purpose: 让仓库自身就能回答“数据放哪、命令怎么写、错误怎么看、结果去哪了”，满足可复现与可交接。
Output: 更新后的 README、两个最小示例脚本、配置契约文档。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md
@.planning/phases/01-experiment-baseline-config-contract/01-RESEARCH.md
@README.md
@config/default_config.m
@main_gotha_bp.m
@cs_echo_recovery/run_cs_echo_recovery_demo.m
@src/bp_read_seed_from_run_dir.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 README 为面向可复现实验的总入口文档</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md, .planning/phases/01-experiment-baseline-config-contract/01-RESEARCH.md, config/default_config.m</read_first>
  <action>重写 `README.md`，把重点放在 Phase 1 已经钉牢的实验基线契约：数据集放置方式、显式 `cfg.path.dataRoot` 用法、主流程和恢复流程入口、主流程默认 headless、输出目录位置、随机种子复用、notebook 只是交互 wrapper。文档必须明确告诉研究者“通过配置覆盖调整实验，不要改生产源码”，并说明运行产物默认不纳入版本控制。</action>
  <verify>Get-Content -Encoding UTF8 README.md | Select-String -Pattern "cfg.path.dataRoot","main_gotha_bp","run_cs_echo_recovery_demo","headless","img","cs_echo_recovery/results"</verify>
  <acceptance_criteria>
    - `README.md` 明确描述数据放置方式与 `cfg.path.dataRoot`
    - `README.md` 明确列出主流程和恢复流程入口
    - `README.md` 明确说明 notebook 只是辅助 wrapper、运行产物默认不纳入版本控制
  </acceptance_criteria>
  <done>研究者只看 README 就能理解正式入口、数据放置、输出位置和基本运行方式。</done>
</task>

<task type="auto">
  <name>Task 2: 添加不改源码即可复用的最小示例脚本</name>
  <files>examples/run_bp_minimal.m, examples/run_cs_recovery_minimal.m</files>
  <read_first>main_gotha_bp.m, cs_echo_recovery/run_cs_echo_recovery_demo.m, config/default_config.m, cs_echo_recovery/cs_default_config.m</read_first>
  <action>新增 `examples/run_bp_minimal.m` 和 `examples/run_cs_recovery_minimal.m`。两个示例都要展示“通过构造 `userCfg` / `csCfg` 覆盖默认配置，而不是修改生产源码”的推荐用法；主流程示例必须包含显式 `dataRoot` 写法与 headless 运行写法；恢复示例必须展示如何通过 `csCfg.project` 和恢复模块自身字段做覆盖。示例脚本应尽量简短、可直接复制到 MATLAB 使用。</action>
  <verify>Get-Content -Encoding UTF8 examples\run_bp_minimal.m,examples\run_cs_recovery_minimal.m | Select-String -Pattern "main_gotha_bp","run_cs_echo_recovery_demo","dataRoot","showProgress","showInterruptedEcho"</verify>
  <acceptance_criteria>
    - `examples/run_bp_minimal.m` 通过 `userCfg` 调用 `main_gotha_bp`
    - `examples/run_cs_recovery_minimal.m` 通过 `csCfg` 调用 `run_cs_echo_recovery_demo`
    - 两个示例都不要求用户修改 `config/default_config.m` 或 `cs_default_config.m`
  </acceptance_criteria>
  <done>仓库内存在可直接复用的最小示例，证明配置覆盖已成为正式用法。</done>
</task>

<task type="auto">
  <name>Task 3: 增加配置契约与错误语义参考文档</name>
  <files>docs/config_contract.md</files>
  <read_first>.planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md, src/bp_merge_config.m, src/bp_validate_config.m, README.md</read_first>
  <action>新增 `docs/config_contract.md`，专门说明 Phase 1 的配置契约：哪些场景使用主流程配置覆盖、哪些场景使用恢复配置覆盖、严格失败意味着什么、错误消息为什么必须带字段路径和原因，以及 2-3 个正/反例（例如错拼字段、显式 `dataRoot`、headless 运行）。README 只保留总览，把细节下沉到该文档。</action>
  <verify>Get-Content -Encoding UTF8 docs\config_contract.md | Select-String -Pattern "严格失败","字段路径","dataRoot","showProgess","run_cs_echo_recovery_demo"</verify>
  <acceptance_criteria>
    - `docs/config_contract.md` 明确写出严格失败语义
    - 文档包含至少一个非法配置反例和一个合法配置正例
    - README 可以链接或引用该文档，而无需重复所有细节
  </acceptance_criteria>
  <done>配置边界和错误语义有了稳定、可引用的仓库内文档落点。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 检查 README，确认其单独即可回答“数据放哪、怎么运行、输出去哪”
- [ ] 在 MATLAB 中打开两个 `examples/*.m`，确认它们只通过覆盖 struct 调用正式入口，不需要编辑生产源码
- [ ] 检查 `docs/config_contract.md`，确认其中包含合法示例、非法示例和“严格失败 + 字段路径 + 原因”的说明
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 仓库文档足以支持新研究者按约定运行基线实验
- 示例脚本把“配置覆盖而非改源码”固化为正式工作流
  </success_criteria>

<output>
After completion, create `.planning/phases/01-experiment-baseline-config-contract/01-03-SUMMARY.md`
</output>
