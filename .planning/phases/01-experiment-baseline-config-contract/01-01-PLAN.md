---
phase: 01-experiment-baseline-config-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bp_merge_config.m
  - src/bp_validate_config.m
  - main_gotha_bp.m
  - cs_echo_recovery/run_cs_echo_recovery_demo.m
autonomous: true
requirements:
  - IMG-02
  - QLT-01
user_setup: []
must_haves:
  truths:
    - "主流程入口对未知字段、错拼字段和错误层级的用户配置会在数据加载前立即失败。"
    - "恢复演示入口与主流程入口使用相同的严格配置覆盖语义，而不是另一套宽松 merge。"
    - "失败信息包含完整配置路径和明确原因，不再只有模糊错误。"
  artifacts:
    - path: "src/bp_merge_config.m"
      provides: "带 dotted path 的严格递归覆盖逻辑"
      contains: "bp_merge_config:UnknownField"
    - path: "src/bp_validate_config.m"
      provides: "字段级类型与范围校验"
      min_lines: 80
    - path: "cs_echo_recovery/run_cs_echo_recovery_demo.m"
      provides: "恢复入口复用共享严格 merge"
      contains: "bp_merge_config"
  key_links:
    - from: "main_gotha_bp.m"
      to: "src/bp_merge_config.m"
      via: "userConfig 严格合并后再进入校验"
      pattern: "bp_merge_config\\(config, userConfig\\)"
    - from: "main_gotha_bp.m"
      to: "src/bp_validate_config.m"
      via: "数据加载前 fail-fast 校验"
      pattern: "bp_validate_config\\(config\\)"
    - from: "cs_echo_recovery/run_cs_echo_recovery_demo.m"
      to: "src/bp_merge_config.m"
      via: "恢复 demo 入口复用共享 merge"
      pattern: "bp_merge_config\\(cfg, userConfig\\)"
---

<objective>
收紧主流程与恢复流程的配置模型，消除静默误配置。

Purpose: 把“用户可以通过配置覆盖做实验”变成可信契约，而不是靠源码编辑和试错。
Output: 共享严格 merge 逻辑、字段级错误信息、恢复入口统一的覆盖语义。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md
@.planning/phases/01-experiment-baseline-config-contract/01-RESEARCH.md
@config/default_config.m
@src/bp_merge_config.m
@src/bp_validate_config.m
@main_gotha_bp.m
@cs_echo_recovery/run_cs_echo_recovery_demo.m
@cs_echo_recovery/cs_recovery_pipeline.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 bp_merge_config 升级为严格递归覆盖器</name>
  <files>src/bp_merge_config.m</files>
  <read_first>config/default_config.m, src/bp_merge_config.m, .planning/phases/01-experiment-baseline-config-contract/01-CONTEXT.md</read_first>
  <action>将 `bp_merge_config.m` 改成“以默认配置 struct 为 schema 的严格递归 merge”。仅允许覆盖默认配置中已存在的字段；若 `userCfg` 提供未知字段、错层级字段、或尝试用 struct 覆盖 leaf / 用 leaf 覆盖 struct，立即报错并停止。错误标识使用稳定的 `bp_merge_config:*` 前缀，错误消息必须包含从 `cfg` 开始的完整 dotted path 和具体原因。保留已知 leaf 字段的直接替换与已知 struct 分支的递归合并，不得再 silently add 新字段。</action>
  <verify>Get-Content -Encoding UTF8 src\bp_merge_config.m | Select-String -Pattern "bp_merge_config:UnknownField","bp_merge_config:TypeMismatch","cfg\."</verify>
  <acceptance_criteria>
    - `src/bp_merge_config.m` 包含稳定错误标识 `bp_merge_config:UnknownField`
    - 错误消息构造逻辑会输出以 `cfg.` 开头的完整字段路径
    - 不再存在“对未知字段直接 `cfg.(key) = userValue`”的宽松分支
  </acceptance_criteria>
  <done>未知字段、错拼字段和错层级字段都在 merge 阶段被拦截，并能定位到完整配置路径。</done>
</task>

<task type="auto">
  <name>Task 2: 保持主流程在数据加载前完成字段级 fail-fast 校验</name>
  <files>src/bp_validate_config.m, main_gotha_bp.m</files>
  <read_first>src/bp_validate_config.m, main_gotha_bp.m, .planning/phases/01-experiment-baseline-config-contract/01-RESEARCH.md</read_first>
  <action>清点 `bp_validate_config.m` 现有消息，确保所有保留的字段校验文本都以明确的 `cfg.*` 路径表述，不退化为模糊 “invalid config”。同时确认 `main_gotha_bp.m` 的顺序保持为：默认配置 -> 严格 merge -> `bp_validate_config` -> `bp_data_pipeline`。如有必要，补充入口侧注释或微调代码顺序，但不要把校验延后到数据读取之后。</action>
  <verify>Get-Content -Encoding UTF8 main_gotha_bp.m | Select-String -Pattern "bp_validate_config\\(config\\)","bp_data_pipeline\\(config\\)"</verify>
  <acceptance_criteria>
    - `main_gotha_bp.m` 中 `bp_validate_config(config)` 出现在 `bp_data_pipeline(config)` 之前
    - `src/bp_validate_config.m` 的错误文本继续使用明确 `cfg.*` 路径
    - 不引入任何“先加载数据再回头发现配置非法”的路径
  </acceptance_criteria>
  <done>主流程在任何昂贵计算前就能用字段级信息拒绝非法配置。</done>
</task>

<task type="auto">
  <name>Task 3: 让恢复演示入口复用共享严格 merge</name>
  <files>cs_echo_recovery/run_cs_echo_recovery_demo.m</files>
  <read_first>cs_echo_recovery/run_cs_echo_recovery_demo.m, cs_echo_recovery/cs_default_config.m, cs_echo_recovery/cs_recovery_pipeline.m, src/bp_merge_config.m</read_first>
  <action>移除 `run_cs_echo_recovery_demo.m` 中本地 `localMergeStruct` 的宽松覆盖逻辑，改为直接复用共享的 `bp_merge_config`。保留恢复入口现有的路径初始化和 `cs_recovery_pipeline(cfg)` 调用方式，但让顶层 `csCfg` 覆盖和嵌套 `csCfg.project` 覆盖都遵守与主流程一致的失败语义。如果本地 helper 完全失去用途，则删除它，避免后续再次分叉。</action>
  <verify>Get-Content -Encoding UTF8 cs_echo_recovery\run_cs_echo_recovery_demo.m | Select-String -Pattern "bp_merge_config","localMergeStruct"</verify>
  <acceptance_criteria>
    - `cs_echo_recovery/run_cs_echo_recovery_demo.m` 调用 `bp_merge_config(cfg, userConfig)`
    - 文件中不再保留 `localMergeStruct` 宽松 merge helper
    - 恢复入口不再拥有独立于主流程的配置覆盖语义
  </acceptance_criteria>
  <done>恢复 demo 入口与主流程入口共享同一套严格配置契约。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 在 MATLAB 中调用 `main_gotha_bp(struct('display', struct('showProgess', false)))`，确认在数据加载前失败，且消息包含 `cfg.display.showProgess`
- [ ] 在 MATLAB 中调用 `run_cs_echo_recovery_demo(struct('methd', struct('run1D', false)))`，确认立即失败，且消息包含错误字段路径
- [ ] 在 MATLAB 中调用一个合法轻量覆盖（如 `struct('display', struct('showProgress', false))`），确认能通过 merge/validate，并且若失败也只会失败在后续缺数据阶段而不是配置阶段
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 两个入口都不再吞掉未知配置字段
- 错误信息能够直接指出非法字段路径和原因
  </success_criteria>

<output>
After completion, create `.planning/phases/01-experiment-baseline-config-contract/01-01-SUMMARY.md`
</output>
